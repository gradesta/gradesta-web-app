{
  "name": "gradesta-web",
  "version": "1.0.0",
  "description": "Graph visualizer static site",
  "scripts": {
    "build": "esbuild script.js --bundle --outfile=bundle.js",
    "watch": "esbuild script.js --bundle --outfile=bundle.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
} 